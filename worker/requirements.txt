# Install PyTorch appropriate for your GPU using https://pytorch.org/get-started/locally/
# Then install these into the SAME Python environment.
# The standalone host needs none of these packages.
diffusers>=0.35,<0.41
transformers>=4.45,<5
peft>=0.17,<0.19
accelerate>=1.0,<2
safetensors>=0.4
Pillow>=10
numpy>=1.26
sentencepiece>=0.2
imageio>=2.34
imageio-ffmpeg>=0.5
